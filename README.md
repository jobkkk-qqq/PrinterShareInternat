<div align="center">

# 🖨️ PrintShare · 打印机共享

**基于 TCP 的局域网打印机共享工具 —— 一台主机共享 USB 打印机，局域网任意电脑轻松使用**

绕过 SMB 打印共享的种种报错（`0x0000011b`、`0x00000709`…），无需第三方打印服务器，
单文件部署、开机自启、Web 队列可视化，一键生成客户端安装脚本。

</div>

<p align="center">
  <img alt="Windows" src="https://img.shields.io/badge/Platform-Windows%207%2F8%2F10%2F11-blue?logo=windows&logoColor=white">
  <img alt="Node" src="https://img.shields.io/badge/Node.js-%E2%89%A5%2018-green?logo=node.js&logoColor=white">
  <img alt="Electron" src="https://img.shields.io/badge/Electron-31-purple?logo=electron&logoColor=white">
  <img alt="Version" src="https://img.shields.io/badge/version-0.2.1-orange">
  <img alt="License" src="https://img.shields.io/badge/License-MIT-lightgrey">
  <img alt="Type3 raw" src="https://img.shields.io/badge/协议-RAW%20(TCP%209100)-informational">
</p>

---

## 这是什么？

打印机通过 **USB** 连在主机上。只要在主机运行本服务，局域网内其他电脑就能
用 Windows 自带的功能 ——「**标准 TCP/IP 端口**」—— 把打印任务发给主机，
由主机交给 USB 打印机。**客户机不用装任何共享软件**，只需一条自动生成的安装脚本。

- **主机**：`PrintShare`（系统托盘常驻）在 **TCP 9100** 提供原始打印服务，**TCP 8081** 提供 Web 管理页。
- **客户端**：添加一台 `Standard TCP/IP` 端口打印机，填主机 `IP:9100` 即可使用。

## 特性

- ✅ **纯 TCP 打印共享**，绕开 SMB 共享的 `0x0000011b` / `0x00000709` 等历史顽疾
- ✅ **可靠的磁盘暂存队列 + 串行投递**：同一时刻只向打印后台投递一个任务，多客户端并发生成有序
- ✅ **队列可视化**：排队 / 打印中 / 已完成、可取消、暂停单任务、调优先级、全局暂停
- ✅ **崩溃恢复**：重启后自动续打未完成任务，数据不丢
- ✅ **Web 管理页**（`localhost:8081`）：选打印机、看队列、下载客户端脚本
- ✅ **系统托盘**：开机自启动、暂停 / 继续、复制服务器 IP、清空队列、退出
- ✅ **单文件便携 exe**：复制到任意 Win7–11 机器双击即用，首次自动放行防火墙
- ✅ **客户端一键安装**：单文件 `.bat`，先用记事本可审计，跨 Win7–11 一键建端口 + 建打印机
- ✅ **前导 0 剥离**：自动剔除不同驱动在任务头塞入的 0x00 垃圾字节，避免打印不被识别
- ✅ **驱动协商兼容**：识别驱动塞进作业头的 UEL / `@PJL` / `@EJL` 前导，纸面出现乱码时可一键剥离
- ✅ **零依赖回归测试**：`npm test` 覆盖队列投递、清空已完成、客户端脚本生成（Node 内置 test runner，不用装测试框架）

## 工作原理

```
┌────────────────┐  9100 (原始打印数据/每次连接=一个任务)   ┌─────────────────────────────┐
│  局域网客户端     │ ─────────────────────────────────────► │        PrintShare (主机)        │
│  自带 TCP/IP 端口 │                                      │  收流落盘(磁盘暂存)              │
│  (IP:9100)      │                                      │  单 worker 串行投递             │
└────────────────┘ ◄───────────────────────────────────── │  WinSpool(DocRAW)→本机USB打印机 │
                         8081 (Web 管理页/面向管理员)          └─────────────────────────────┘
```

客户端发送打印任务 = 建立一条到 `主机IP:9100` 的 TCP 连接并写完数据。
主机把**每个连接流式写入磁盘暂存队列**，再由**单个 worker 按序串行提交**给 Windows 打印后台
（通过 `winspool.drv` 的 `OpenPrinter / StartDoc / WritePrinter / EndDoc`，PowerShell P/Invoke，零原生依赖），
最终把 raw 数据交给 USB 打印机。

## 快速开始（用打包好的 exe）

### 1️⃣ 主机端

1. 把 `PrintShare *.exe` 复制到主机（本机接好 USB 打印机，驱动已装）。
2. **双击运行**：首次会自动弹一次 UAC 授权，放行防火墙（TCP `9100`），出现托盘图标。
   （管理页 `8081` 只监听本机，不需要放行，见下方[安全设计](#安全设计)。）
3. 打开管理页 <kbd>http://localhost:8081</kbd>，在页面上选中本机**实际连接的打印机**并保存。

### 2️⃣ 客户端

两种方式任选：

<details>
<summary><b>A. 手动添加（通用）</b></summary>

- 「设置 → 蓝牙和其他设备 → 打印机和扫描仪 → 添加设备」
- 选「**通过 TCP/IP 地址或主机名添加打印机**」
- IP 填主机的**局域网 IP**；端口默认 `9100`；取消勾选“查询打印机自动选择驱动”
- 选择与该型号匹配的**厂商驱动**完成添加

> 客户机必须安装与主机同型号的**厂商 Type-3 完整驱动**，否则可能出现乱码 / 白纸。
</details>

<details>
<summary><b>B. 客户端一键脚本（推荐）</b></summary>

- 在主机管理页点「**下载 zip 安装包**」
- 把 zip 拷到客户机，解压后用**记事本打开** `安装共享打印机.bat` 核对服务器 IP / 驱动
- **右键以管理员身份运行**：脚本会先验证服务器可达、检查驱动，仅在确需建打印机时才请求提权，
  删除旧同名打印机前先询问，**失败不破坏原配置**
- 脚本不自动设默认打印机，可按需要在系统设置里手动设为默认
</details>

### 3️⃣ 日常管理

右键托盘图标：开机自启动 / 暂停队列 / 复制服务器 IP / 清空已完成 / 退出。
管理页可随时查看队列、暂停、取消、调优先级。

## 从源码构建 / 运行

需要 **Node.js ≥ 18**（`npm test` 的内置测试运行器需 18.13+）。

```bash
npm install          # 安装依赖（自动生成图标）

npm start            # 命令行跑打印服务（联调用）
npm run electron     # 带系统托盘运行
start-server.bat     # 一键启动（检测 Node、放行防火墙、打开管理页）

npm test             # 回归测试：队列 / 清空已完成 / 客户端脚本生成
npm run build        # 打包单文件便携版 exe → dist\PrintShare *.exe
```

> `build.bat` 已内置国内网络所需的镜像配置（nsis 等构建工具走 npmmirror），并把构建缓存放在
> 项目内 `.buildcache\`，保证一键出包。

联调模式（仅走队列流程、不真正打印）：
```bash
set DRYRUN=1   # 然后 node server.js
```
该模式下所有 PowerShell / WMI 查询都返回固定值，没装打印机、没权限的机器也能跑通完整队列流程（`npm test` 就靠它做到零依赖）。

## ⚙️ 配置

| 项 | 说明 | 默认 |
| --- | --- | --- |
| `RAW_PORT` | 客户端打印连接端口（环境变量） | `9100` |
| `ADMIN_PORT` | Web 管理页端口（环境变量） | `8081` |
| `ADMIN_BIND` | 管理页监听地址（环境变量）；设 `0.0.0.0` 才允许局域网访问管理页 | `127.0.0.1`（仅本机） |
| `PRINTSHARE_DATA_DIR` | 队列 / 配置目录（环境变量，优先级最高） | 源码运行=项目目录；打包运行=`%APPDATA%\PrintShare` |
| `config.json` | 目标打印机名（首次运行自动创建，可缺省） | 自动取默认打印机 |
| `queue/` | 磁盘暂存的任务队列；`meta.json` 存元数据 | 自动创建 |

> **数据放哪？** 源码方式运行（`npm start` / `npm run electron`）时，`config.json` 与 `queue/` 就在项目目录下；
> **打包版 exe 运行时**改放 `%APPDATA%\PrintShare\`。因为便携 exe 每次启动都会解压到临时目录，
> 写在 exe 旁边会随重启丢失（队列、已选打印机、剥离开关都会重置），放 `%APPDATA%` 才能真正持久化。
> 也可以用 `PRINTSHARE_DATA_DIR` 显式指定（自动化测试就是用它把数据隔离到临时目录的）。

## 安全设计

本项目的安装脚本与服务刻意做到**最小权限、可审计、不破坏原配置**：

- 生成给客户机的 `安装共享打印机.bat` 是**可读纯文本**（非 Base64 混淆），能用记事本核对 IP / 驱动
- 用系统内置的 `prnport.vbs` / `prnmngr.vbs`（经 `cscript`），**不依赖 Win8+ 才有**的
  `Get-Printer` / `Add-Printer`，因此跨 **Win7 → Win11** 全系可用
- 提权（UAC）**只在真正需要创建 / 删除打印机时**触发；删除前先询问，且只操作本脚本命名的对象
- 创建端口前先用 TCP 探测验证服务器可达
- 服务端投递前自动**剥离任务开头连续 0x00**（不同驱动前导 0 长度不同，动态适配）
- 管理页的写接口只接受 `application/json` 且校验 `Origin`，其它网页无法在你的浏览器里对
  `localhost:8081` 发起跨站写操作
- 管理页**默认只监听 `127.0.0.1`**（本机），因此不需要、也不会为 `8081` 放行防火墙；
  打印端口 `9100` 照常对局域网开放

> ⚠️ **管理页没有登录口令**：一旦用 `ADMIN_BIND=0.0.0.0` 开放给局域网，能打开 `8081` 的人
> 就能改共享打印机、清空队列。只有确实需要从别的电脑管理时才这样开，并且自己放行 `TCP 8081`。

## 技术栈

| 层 | 技术 |
| --- | --- |
| 打印服务 | Node.js `net` / `http` 原生模块，TCP 9100 + Web 8081 |
| Spooler 投递 | PowerShell P/Invoke → `winspool.drv`（RAW，零原生依赖） |
| 桌面外壳 | Electron + 系统托盘 + 开机自启动（注册表 `Run` 键） |
| 打包分发 | electron-builder → 单文件 portable exe（x64） |

## 目录结构

```
main.js            Electron 主进程：托盘菜单 + 开机自启动 + 启动服务
server.js          核心打印服务：TCP 9100 收流 → 磁盘队列 → 串行投递 → WinSpool
winspool.ps1       PowerShell P/Invoke，把数据交给本机打印机（零原生依赖，含前导0剥离）
public/index.html  Web 管理页：打印机选择、队列可视化、客户端脚本生成
make-icon.js       生成托盘/应用图标（ico/png）
build.bat          一键打包脚本（内置镜像与缓存路径）
start-server.bat   一键启动脚本（纯 ASCII，检测 Node、放行防火墙、打开管理页）
test/              零依赖回归测试（node --test：队列 / 清空已完成 / 客户端脚本生成）
```

## ❓ 常见问题

- **客户端脱机**：确认主机已放行 `TCP 9100`。便携 exe 首次启动会自动弹 UAC 放行；若点过「否」或用旧版，
  可右键 `start-server.bat` 以管理员运行补放行。
- **打不开管理页**：管理页只在**主机本机**提供（`http://localhost:8081`），默认不对局域网开放，
  因此也没有放行防火墙。确实要从别的电脑管理时：在主机设 `ADMIN_BIND=0.0.0.0`，再自行放行 `TCP 8081`
  ——该页面没有登录口令，谁能访问谁就能改配置。
- **任务显示“已完成”但打印机不动**：多半是硬件（缺纸 / 缺墨盒），或打印机语言与驱动不匹配。
  本版已在投递前剥离前导 0，若仍异常可在管理页看队列里的字节数是否合理。
- **纸张顶部打印出 `284.4@EJL`（爱普生）/ `@PJL`（HP）等乱码，正文被下推**：驱动与打印机之间的
  **作业语言协商**没对上，详见下方[《驱动 ↔ 打印机协商问题》](#驱动--打印机协商问题纸面乱码--正文下移)。
- **安装共享打印机.bat 在 Windows 7 下闪退 / 报「不是内部或外部命令」**：旧版脚本有两个问题：
  ① 以 UTF-8 编码并在开头执行 `chcp 65001`，Win7 的 cmd 解析 UTF-8 批处理时会把行尾中文误读、
  直接中止脚本；② 把**队列名**（可能是中文，如「M227fdw 财务」）当作驱动名写进脚本，并且设置默认
  打印机的那行提示里 `Bluetooth & devices` 的 `&` 没转义，被 cmd 当成命令分隔符，凭空多出两条
  报错。新版已修：脚本**纯 ASCII（英文界面）**、不依赖任何代码页，注入值统一收敛为 ASCII，
  `&` 一律转义；驱动名改为按队列名反查 **`Win32_Printer.DriverName`（驱动型号名）**，
  既保证脚本是 ASCII，也修掉了 `prnmngr.vbs -m` 传队列名导致「驱动未安装」的误判。
  可在 Win7/8/10/11、中文/英文系统上一致运行——请重新下载 zip 安装包。
- **管理页打印机列表里中文名显示成乱码（`M227fdw ���`），或打印到中文名打印机时报找不到打印机**：
  旧版打包 exe 以 GUI 方式运行时**没有控制台**，它调用的 PowerShell 会用系统 OEM 代码页写 stdout，
  而 Node 按 UTF-8 读取，中文名就被读成 U+FFFD。乱码名会一路污染打印目标与驱动型号查询。
  新版已修：所有要读文本的 PowerShell 调用都先强制 `[Console]::OutputEncoding=UTF8`——请重新打包/下载。
- **端口被占**：管理页默认 `8081`、打印端口 `9100`，可用 `ADMIN_PORT` / `RAW_PORT` 环境变量更换。
  管理页端口被占用时打印服务照常工作（只记日志）；打印端口被占用时托盘版会弹框说明原因。
- **开机自启动不生效**：复选框状态来自注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`
  的值 `PrintShare`；必须是便携版 exe 的真实路径（代码已自动处理便携版解压到临时目录的情况）。
- **客户端乱码 / 白纸**：客户机未装与主机同型号的厂商完整驱动。请先安装对应驱动再打印。

### 驱动 ↔ 打印机协商问题（纸面乱码 / 正文下移）

驱动经 TCP/IP 端口打印时，会在作业开头插入一段**作业语言**同步命令。打印机不支持（或对应模式被关）时，
这段命令不会被识别，而是被当正文原样打印出来 —— 纸面出现乱码、正文被向下推、版式与本地打印不一致。
各品牌的对应关系与解法：

| 品牌 | 纸面表现 | 根因 | 客户端解法（首选） |
| --- | --- | --- | --- |
| **爱普生** | `284.4@EJL` | EJL（Epson Job Language）打包模式同步命令，形如 `ESC 01 @EJL 1284.4` | 打印机属性 → **设备设置 → 打包模式 = 关**；驱动无此项时，把打印机缺省设置中的 Packet 模式改为**自动** |
| **HP** | `@PJL` 等控制文本 | 作业被 PJL 包裹在 UEL（`ESC%-12345X`）之间，非 PJL 打印机把命令按 ASCII 文本打印 | 确认机型驱动正确；打印机语言 / **Personality 设为 Auto**；优先用 PCL5/PCL6 或 PostScript 驱动（HP 通用驱动不支持宿主型设备） |
| **佳能** | 乱码 / 版式错 | 官方归因为**驱动型号选错**；CAPT 属宿主型驱动，依赖佳能专有数据流 | 安装与本机型完全对应的驱动；跨 TCP/IP 转发优先改用 PCL/PS 或 UFR II 驱动 |

**通用项**：客户端「打印机属性 → 端口 → 取消勾选 **启用双向支持**」。本中继是单向的，双向状态查询只会让打印卡顿。

参考来源：[爱普生 FAQ 310058](https://www.epson.com.cn/faq/7be73cb7671b4394a456dab636ec7937.html) ·
[HP PJL Technical Reference Manual](https://developers.hp.com/sites/default/files/PJL_Technical_Reference_Manual.pdf) ·
[Canon 打印乱码排障](https://oip.manual.canon/USRMA-0620-zz-CS-enUS/contents/CS2545_trouble_0801_identifyingandisolatingproblems.html)

**程序侧兜底**：主机管理页有「驱动协商兼容」卡片。转发时若在作业开头识别到 UEL / `@PJL` / `@EJL` 前导，
会在队列里给该任务打上「协商」标记，并在控制台给出提示。客户端太多、无法逐台改驱动时，可打开卡片里的
**「剥离作业语言前导」**开关，由主机在投递前自动剥掉作业开头的 UEL / `@PJL` / `@EJL` 前导块与结尾 UEL
（不改动正文）。**默认关闭** —— 打印机本身支持 PJL 时开启反而会丢掉作业设置。

生成的 `安装共享打印机.bat` 头部会按目标驱动品牌自动附上对应提示（爱普生 / HP / 佳能 / 通用）。

## ✅ Roadmap

- [x] TCP 打印共享核心 + 可靠性队列
- [x] Web 管理页与队列可视化
- [x] 系统托盘 + 开机自启动 + 单文件打包
- [x] 客户端一键安装脚本（跨 Win7–11 纯 BAT）
- [x] 前导 0 剥离修复
- [x] 驱动协商兼容：EJL / PJL / UEL 前导检测与可选剥离（爱普生 / HP / 佳能）
- [x] 零依赖回归测试（`npm test`：队列投递 / 清空已完成 / 客户端脚本生成）
- [ ] 打印页数 / 耗材状态上报
- [ ] 打印机多实例 / 一台主机管理多台打印机

## 📝 更新日志

### 0.2.1

**修复**

- **「清空已完成」会误删排队任务的数据文件**：原实现是"先过滤掉已完成任务、再对剩下的任务删文件"，
  正好删在排队中 / 打印中的任务上，队列里会留下永远打不出来的空任务。现在只删被清掉那批任务的文件，
  并返回删除条数；启动加载元数据时若发现排队任务的数据文件缺失，直接标记为「数据文件丢失」，
  不再拿一个空任务去投递
- **还没选打印机时收到的任务会被直接判失败**：现在保持排队等待，在管理页选好打印机后自动继续，不丢件
- **客户端中途断连会留下半截 `.ing` 文件**：原来在写盘流还没关闭时就 `unlink`，Windows 下必然失败，
  半截文件永久残留；改成等写入流关闭再删，并在启动时清掉遗留的 `.ing`
- **生成的安装脚本行尾不稳定**：源码在 Windows 检出为 CRLF 时，`replace(/\n/g, '\r\n')` 会把每一行变成
  `\r\r\n`；现在先归一到 `\n` 再转 `\r\n`，输出恒为纯 ASCII + 标准 CRLF
- **管理页端口被占用会连累打印服务**：管理页起不来现在只记日志（打印端口才是关键路径）；
  打印端口被占用时，托盘版会弹框说明原因，不再"双击了没反应"
- **便携版可以重复启动**：两份实例共用 `%APPDATA%\PrintShare` 的队列文件会互相覆盖、丢失元数据，
  现在加了单实例锁，第二份实例只唤起已运行实例的管理页
- 管理页不再每 2.5s 重建打印机下拉（原来会关掉正在展开的列表、把"已选好但还没保存"的值顶回去）
- 没有系统默认打印机时，管理页刷新不再每次都起一个 PowerShell 进程

**安全**

- 管理页写接口只接受 `application/json` 并校验 `Origin`，其它网页无法在你浏览器里对 `localhost:8081`
  发起跨站写操作
- 管理页**默认只监听 `127.0.0.1`**（本机），不再为 `8081` 放行防火墙；确实需要远程管理时设
  `ADMIN_BIND=0.0.0.0`（该页面没有登录口令，开放即等于谁能访问谁就是管理员）

**新增**

- `PRINTSHARE_DATA_DIR` 环境变量：显式指定队列 / 配置目录
- `npm test`：零依赖回归测试（Node 内置 test runner），覆盖队列投递与完成、清空已完成的删除范围、
  取消任务、未选打印机不丢件、客户端脚本生成（纯 ASCII / CRLF / 注入值收敛 / 标签成对）、
  作业语言前导剥离、管理页跨站防护；全程跑在 `DRYRUN` + 临时数据目录里，不碰真实打印机与项目数据
- `DRYRUN=1` 联调模式不再调用 PowerShell / WMI，没装打印机的机器也能跑通完整队列流程

### 0.2.0

**新增 · 驱动 ↔ 打印机协商兼容**

- 识别作业开头的作业语言前导：UEL（`ESC%-12345X`）、HP `@PJL`、爱普生 `@EJL`（含 `ESC 01 @EJL 1284.4`）
- 命中时在队列里给任务打「协商」标记，并在控制台提示品牌与对应解法
- 管理页新增「驱动协商兼容」卡片：显示检测到前导的任务数、队列标记，以及
  **「剥离作业语言前导」**开关（默认关闭）—— 开启后由主机在投递前剥掉前导块与结尾 UEL，不改动正文
- 生成的 `安装共享打印机.bat` 头部按目标驱动品牌自动附上排查提示（爱普生 / HP / 佳能 / 通用）

**修复**

- **安装脚本在 Win7 闪退**：脚本改为**纯 ASCII（英文界面）**、不依赖任何代码页，不再执行 `chcp 65001`；
  注入值统一收敛为 ASCII、`&` 一律转义；驱动名改为按队列名反查 `Win32_Printer.DriverName`（驱动型号名），
  修掉「把中文队列名当驱动名」导致 `prnmngr.vbs -m` 匹配失败的问题
- **管理页中文打印机名乱码**：无控制台环境（打包版 GUI）下 PowerShell 以 OEM 代码页写 stdout，
  导致中文名被读成 U+FFFD 并污染打印目标与驱动型号查询；现所有读取文本的 PowerShell 调用都先强制
  `[Console]::OutputEncoding = UTF8`
- `start-server.bat` 同步改为纯 ASCII + CRLF，Win7 下不再闪退

**变更**

- 便携版 exe 的 `config.json` 与 `queue/` 改存 `%APPDATA%\PrintShare\`（原先写在临时解压目录，
  每次重启都会丢队列与已选打印机）；源码方式运行仍在项目目录下
- 打包不再把 `queue/` 目录塞进 exe

## License

[MIT](./LICENSE) © PrintShare

---

<p align="center">有问题 → <a href="https://github.com/jobkkk-qqq/PrinterShareInternat/issues">提交 Issue</a> | ⭐ 如果对你有用，欢迎点个 Star</p>
